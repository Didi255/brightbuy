/**
 * cart slice — OWNER: M3
 * Copy the shape of modules/auth/. Mount this router in src/app.js when ready.
 */
const router = require('express').Router();

// TODO(M3): define routes here

module.exports = router;
